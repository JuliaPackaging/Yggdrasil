--- configure.ac	2013-11-13 12:57:50.000000000 -0500
+++ /tmp/configure.ac	2020-12-01 17:22:36.000000000 -0500
@@ -237,9 +237,9 @@
         libzmq_on_mingw32="yes"
         libzmq_dso_visibility="no"
 
-        if test "x$enable_static" = "xyes"; then
-            AC_MSG_ERROR([Building static libraries is not supported under MinGW32])
-        fi
+#        if test "x$enable_static" = "xyes"; then
+#            AC_MSG_ERROR([Building static libraries is not supported under MinGW32])
+#        fi
         ;;
     *cygwin*)
         # Define on Cygwin to enable all library features
@@ -332,8 +332,8 @@
 
 pgm_basename="libpgm-5.2.122~dfsg"
 
-AC_ARG_WITH([pgm], [AS_HELP_STRING([--with-pgm], 
-    [build libzmq with PGM extension [default=no]])], 
+AC_ARG_WITH([pgm], [AS_HELP_STRING([--with-pgm],
+    [build libzmq with PGM extension [default=no]])],
     [with_pgm_ext=$withval], [with_pgm_ext=no])
 
 # build using system pgm
