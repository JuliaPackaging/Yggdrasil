--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -367,7 +367,7 @@
     endif()
 
     # deal with zlib
-    if (WIN32)
+    if (FALSE)
       find_package(Boost ${RDK_BOOST_VERSION} COMPONENTS zlib CONFIG)
       if(Boost_zlib_FOUND)
         set(zlib_lib Boost::zlib)
