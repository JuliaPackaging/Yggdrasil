diff --git a/src/runtime_src/core/common/detail/windows/utils.h b/src/runtime_src/core/common/detail/windows/utils.h
index 1a6eda981..91337a7c0 100644
--- a/src/runtime_src/core/common/detail/windows/utils.h
+++ b/src/runtime_src/core/common/detail/windows/utils.h
@@ -52,16 +52,19 @@ sys_dep_get_last_err_msg()
 inline std::string
 getenv(const char* name)
 {
+#ifdef _MSC_VER
   char* value = nullptr;
   size_t len = 0;
 
-  // _dupenv_s allocates memory for the value
   if (_dupenv_s(&value, &len, name) != 0 || value == nullptr)
     return {};
-  
-  // Use unique_ptr to ensure memory is freed even if string constructor throws
+
   std::unique_ptr<char, decltype(&std::free)> guard(value, &std::free);
   return std::string(guard.get());
+#else
+  const char* value = std::getenv(name);
+  return value ? std::string(value) : std::string{};
+#endif
 }
 
 inline std::string
