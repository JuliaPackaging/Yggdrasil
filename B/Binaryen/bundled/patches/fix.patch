index fb4bb24..b90c9c4 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -290,6 +290,7 @@ if(MSVC)
 else() # MSVC

   add_compile_flag("-fno-omit-frame-pointer")
+  add_compile_flag("-D__STDC_FORMAT_MACROS")
   if(NOT BUILD_FUZZTEST)
     # fuzztest depends on RTTIs.
     add_compile_flag("-fno-rtti" "CXX_ONLY")
