diff --git a/profiler/CMakeLists.txt b/profiler/CMakeLists.txt
--- a/profiler/CMakeLists.txt
+++ b/profiler/CMakeLists.txt
@@ -30,13 +30,22 @@ include(${CMAKE_CURRENT_LIST_DIR}/../cmake/vendor.cmake)
 include(${CMAKE_CURRENT_LIST_DIR}/../cmake/server.cmake)
 
 include(ExternalProject)
-ExternalProject_Add(embed
-    SOURCE_DIR ${CMAKE_CURRENT_LIST_DIR}/helpers
-    CMAKE_ARGS
-        -DCMAKE_BUILD_TYPE=Release
-        -DCMAKE_INSTALL_PREFIX=${CMAKE_CURRENT_BINARY_DIR}
-)
+# For cross-compilation: if embed already exists (pre-built with host compiler),
+# skip ExternalProject to avoid rebuilding it for the target architecture.
+if(EXISTS ${CMAKE_CURRENT_BINARY_DIR}/embed)
+    message(STATUS "Using pre-built embed tool for cross-compilation")
+    add_custom_target(embed)
+else()
+    ExternalProject_Add(embed
+        SOURCE_DIR ${CMAKE_CURRENT_LIST_DIR}/helpers
+        CMAKE_ARGS
+            -DCMAKE_BUILD_TYPE=Release
+            -DCMAKE_INSTALL_PREFIX=${CMAKE_CURRENT_BINARY_DIR}
+    )
+endif()
 
+# Generate embedded data files (fonts, manual, prompts)
 add_custom_command(
     OUTPUT data
     COMMAND ${CMAKE_COMMAND} -E make_directory data

