--- a/configure	2024-09-03 17:08:23.323524834 +0200
+++ b/configure	2024-09-03 17:08:33.727532497 +0200
@@ -6037,38 +6037,7 @@
 fi
 
 
-
-{ printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking if popcnt instruction is available and requested" >&5
-printf %s "checking if popcnt instruction is available and requested... " >&6; }
-if test "$cross_compiling" = yes
-then :
-  { { printf "%s\n" "$as_me:${as_lineno-$LINENO}: error: in '$ac_pwd':" >&5
-printf "%s\n" "$as_me: error: in '$ac_pwd':" >&2;}
-as_fn_error $? "cannot run test program while cross compiling
-See 'config.log' for more details" "$LINENO" 5; }
-else case e in #(
-  e) cat confdefs.h - <<_ACEOF >conftest.$ac_ext
-/* end confdefs.h.  */
-
-int
-main (void)
-{
-if (__builtin_cpu_supports("popcnt")) return 0; else return 1;
-  ;
-  return 0;
-}
-_ACEOF
-if ac_fn_c_try_run "$LINENO"
-then :
-  popsup=1
-else case e in #(
-  e) popsup=0 ;;
-esac
-fi
-rm -f core *.core core.conftest.* gmon.out bb.out conftest$ac_exeext \
-  conftest.$ac_objext conftest.beam conftest.$ac_ext ;;
-esac
-fi
+popsup=0
 
 
 if test "$allow_popcnt" -eq 1
