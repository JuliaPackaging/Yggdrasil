--- a/meson.build
+++ b/meson.build
@@ -113,6 +113,6 @@ wayland_protocols = declare_dependency(
 
 meson.override_dependency('wayland-protocols', wayland_protocols)
 
-if get_option('tests')
-	subdir('tests')
-endif
+# if get_option('tests')
+# 	subdir('tests')
+# endif
